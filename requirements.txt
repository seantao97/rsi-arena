# Runtime
httpx>=0.27
pydantic>=2.7

# Backend (server/). Not needed to use rsi_arena as a library.
fastapi>=0.115
uvicorn[standard]>=0.30

# Tests. `pip install -e ".[dev]"` installs these too.
pytest>=8.0
pytest-asyncio>=0.23
pytest-cov>=5.0
